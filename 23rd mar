what is ingress limitations of ingress
Ingress:
Ingress is a Kubernetes resource used to expose HTTP/HTTPS services from outside the cluster.
Ingress = rules to route external traffic to internal services
Ingress is a Kubernetes API object that defines how external HTTP/HTTPS traffic is routed to services inside a cluster.
It works together with an Ingress Controller (like NGINX, Traefik, HAProxy) that actually handles the traffic. [gateway-ap...igs.k8s.io]


worked on gateway api created routes for difffernet microservice application having both pfront end backend
  wrote gateway yaml file
http routes etc..
