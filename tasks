19-
gave us 5 image for it we had to write the deployment file.(static app)
service file
and config file
and then k gateway setup and 
http routes then deploy it and see

23
gave us 3 repository having full stack project 
made necesaarry changes like chnaging the service type
created the gateway configuration
one common for all three differnt applicaiton
Then wrote Http routes for it
and in the ha proxy pentioned everything configuration and was able too my app running
retail-project
fitnesss
hospital manegemnt

24th
configured volumes for the same 3 repositores 
first emty dir
then host path volumes
and then tryied to get OOM error using shell scripting 
25th
configured nfs server in a different instance 
then in the client(worker node installed nfs common 
then in the configuration file mention the ip address of worker node
and in worker node i mapped the path to remote
 then we were given a task to create pv and pvc files for our fitness app application and retail project application for both mongo db and mysql
there we monted the path to persistentVolumeClaim

same thing we did using dynamic provisioning
created storage class
pvc configuration (usind in-tree driver)

27th
---
from scrath we implemented an application for duinversity project which had only source code
we built the image for it
wrote the deployment files
service files
gateway files
config maps and secrets
http routes
it had mysql database
created pvc files and storage class
i installed the csi driver and moutned the volumes to the nfs server and also the whole process with the help of sub dir or in tree kubernetes driven driver
so observed the difference between both storage class

28th
----
for the above application only i implemented statefulsets headless servicees
and we didnt need the pvc files we can mention that in the volume claim tempelate.
and then learned about probes 
all three types of probes implemented for the same project deploymenet
and observed it.


30th
----
helmiified the whole application of Duuniversity
Created a tempelate chart
values.yaml file
tried to parameterised the required values
installed helm in our control plane after cloning the repo 
and then installed prometheus and grafana and the node port of the loadbalacer i configured it into the ha proxy 
